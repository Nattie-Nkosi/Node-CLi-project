# Node CLI command project _node-ls_

A clone of "ls command in Linux, Mac and Windows based system terminal to list files in current folder. I created my own `ls` command.

### Tech Stack

- Node
- NPM Modules (chalk)
